// Placeholder to satisfy Jest test discovery.
// Actual tests are in usePreviewMode.test.tsx
describe('usePreviewMode (placeholder)', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
